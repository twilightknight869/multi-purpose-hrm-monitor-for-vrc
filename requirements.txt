# ============================================================
#  HRM Monitor — Python Dependencies
#  Questions? Discord: crimsondreamz
# ============================================================
#
# QUICK INSTALL (recommended — runs everything below at once):
#   Double-click install_deps.bat
#
#   OR run in a terminal:
#   pip install -r requirements.txt
#
# ============================================================

# ── Core (required) ─────────────────────────────────────────
PyQt6>=6.6.0
websocket-client>=1.7.0

# ── VRChat OSC / chatbox / status rotation (optional) ───────
python-osc>=1.8.0

# ── SteamVR wrist overlay (optional) ────────────────────────
openvr>=1.26.701

# ── Spotify Now Playing (optional) ──────────────────────────
spotipy>=2.23.0

# ── Friend HR sharing ────────────────────────────────────────
# No extra packages needed — uses Ably Realtime over port 443
# (pure Python urllib + urllib.request, built into Python).
# Get a free Ably API key at https://ably.com and paste it
# in HRM Monitor Settings → Friend HR Sharing.
